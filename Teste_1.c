#include <stdio.h>

int main(void); {
    // Entrada
    int a = 10;
    int b = 20;

    // Processamento
    if (a > b) {
        // Saida
        printf("a = %d > b = %d" a, b);
    }
    else (a < b) {
        // Saida
         printf("a = %d <= b = %d\n", a, b);
    }


    return 0;

}